# DECISIONS.md — 모두의 마라톤(P5 · 앱명 『5키로』) 결정 로그

> 같은 논쟁을 두 번 하지 않기 위한 기록. 형식: `날짜 · 등급 · 무엇을 · 왜 · 포기한 것`
> 등급 = **L1** 팀원 재량 / **L2** 팀장 결정 / **L3** 회장 결정(돈·대외발행·법적리스크·프로젝트항목)
> 진행 상황은 [`JN.md`](JN.md), 고정 정보는 [`CLAUDE.md`](CLAUDE.md).

---

## 2026-07-30 (세션10)

**L2 · 공유 카드를 "화면 캡처"가 아니라 1080px SVG **재렌더**로** (메인 세션)
왜: 캡처(`react-native-view-shot`)는 해상도가 **기기 화면 밀도에 묶인다** — 검증 기준기가 320dp
폭이라 거기서 뽑은 이미지는 인스타에서 흐리다. `react-native-svg`는 이미 쓰고 있고, `viewBox`를
준 Svg는 `toDataURL(w,h)`에서 캔버스 크기로 viewBox 행렬을 다시 계산해 정확히 스케일된다
(네이티브 `SvgView.drawChildren` 확인 후 채택).
포기한 것: RN 레이아웃 엔진. SVG엔 없어서 글자 폭을 직접 계산해야 하고, **넘친 글자가 조용히
잘린다**. 대신 폰트 `hmtx`에서 advance width를 실측해 쓴다(`lib/text-metrics.ts`).

**L2 · 실기기 없이 도는 레이아웃 검산 스크립트 도입** (메인 세션)
왜: 좌표가 절대값이라 한 번 틀리면 APK 빌드 20분을 태운 뒤에야 안다. 최악 케이스(마라톤
42.19km · 20자 러너 네임 · 6시간 러닝)를 산술로 돌리는 `scripts/check-share-card.ts`를 만들었고,
**첫 실행에서 바로 결함 1건**을 잡았다(헤더에 "이름 · 날짜"를 한 줄로 붙였더니 20자 이름이
워드마크에 닿음 → 두 줄로 분리).
포기한 것: 눈으로 확인하는 방식. 단 이 스크립트는 **좌표만** 본다 — 폰트가 실제로 적용됐는지,
색 대비가 맞는지는 여전히 실기기 캡처로만 알 수 있다.

**L2 · 사진을 넣으면 거리 링을 그리지 않는다** (메인 세션, 실기기 캡처가 잡음)
왜: 링이 사진 피사체의 **얼굴을 정확히 가로질렀다**. 사진을 넣었다는 건 "이 순간을 보여주겠다"는
뜻이라 그래픽이 주인공을 가리면 안 된다.
포기한 것: 모든 모드에서 같은 구성을 쓰는 단순함. (GPS 경로는 정보 가치가 있어 사진 위에도 유지.)

**L2 · 공유 카드에는 "다크 카드 = 랭킹 시상대 전용" 규칙을 적용하지 않는다** (메인 세션)
왜: 그 규칙은 **앱 탭 화면**이 같은 템플릿으로 반복되는 걸 막으려는 것이다. 공유 카드는 앱 밖으로
나가는 이미지라 대상이 아니다. 기본은 크림이고 **사진을 넣었을 때만** 다크 스크림이 깔린다.
포기한 것: 규칙의 문자 그대로의 적용. 대신 근거를 `DESIGN.md`에 남겨 다음 감사에서 재논쟁하지
않게 했다.

## 2026-07-30 (세션9)

**L3 · 팔레트를 크림+포레스트그린으로 전면 교체** (회장)
왜: 회장 판단 "너무 파란색이 위주, 아예 새로". 세션8 독립 채점도 "블루+화이트+그레이뿐인 팔레트
= v0/Bolt류 AI 스캐폴딩 문법"을 "AI스럽다"의 3대 원인 중 하나로 지목했다.
포기한 것: 2026-07-12에 확정한 Azure Blue `#2563c9`(오렌지→블루 리브랜딩의 결과물). 안 B(코랄)는
브랜드색과 골드가 둘 다 웜톤이라 **골드의 "특별한 순간" 신호가 희석**돼서, 안 C(딥틸)는 청록이라
"아예 바꾸라"는 지시를 부분적으로만 만족시켜서 탈락.

**L3 · 마스코트를 사람 러너 → 양 『오키』로 교체** (회장)
왜: 회장이 카카오 이모티콘풍 레퍼런스를 제시. 이름은 슬로건 "오늘 5키로, **오키**?"와 엄지척
포즈가 이미 맞물려 있어 **새로 짓지 않고 승격**했다.
포기한 것: 수달(2순위 — 제가 실행 실패로 잠시 추천했으나 회장이 양으로 확정). 진돗개는
**카카오프렌즈 "프로도"가 이미 진돗개 모티브**라 IP 리스크로 보류(변리사 방수민 스팟체크).

**L3 · 서지안(UX)의 반대 의견을 무시하지 않고 "시안만 먼저" 로 결정** (회장)
왜: 황태경(찬성) vs 서지안(반대)이 정면으로 갈렸다. 서지안 근거 = 세션8이 **색을 안 바꾸고**
79→94를 만들었으니 색이 원인이 아니었다 + PRD가 런데이를 이미 "디자인 혹평"으로 분류 + 3050
남성이 공유하기 부끄러울 위험. 추상적 찬반으로는 결론이 안 나서 **싸고 되돌릴 수 있는 검증**을 택했다.
포기한 것: 즉시 전면 착수(리스크 큼) / 즉시 보류(회장의 심미 판단을 채점표보다 가볍게 취급).

**L2 · 시상대 다크를 자문안 `#26301f` → `#171c12` 로 보정** (메인 세션, 황태경 동의)
왜: `#26301f`에서는 **골드가 4.30:1로 AA 미달**(현행 네이비에선 5.41:1). 세션8이 다크 위 블루
3.06:1을 잡아 `brandOnDark`를 만든 것과 같은 종류의 함정.
포기한 것: 자문안 수치를 그대로 따르는 편의.

**L2 · `Team.green` 대체안(머스터드·틸) 둘 다 폐기, 문제는 미해결로 명시** (메인 세션)
왜: 머스터드 `#c78a2e`는 골드와 **1.08:1 = 사실상 같은 색**이라 "골드=순위·성과 전용" 규칙을 깬다.
틸 `#2f8f8a`는 브랜드 그린과 **색상각 26°**밖에 안 벌어져 현행과 다를 게 없다.
포기한 것: 억지로 색을 바꿔 규칙을 깨는 것. 대신 코드에 미해결로 박아두고 **파일럿에서 눈으로
확인**하는 쪽을 택했다 — 결과적으로 진짜 원인은 `Team.green`이 아니라 `RINGS[0] = Brand.brand`였다.

**L2 · 아바타 링 3색을 레드·플럼·슬레이트로 재설계** (메인 세션, R18/R19 채점 지적)
왜: 기존 `RINGS`가 **브랜드 액션색 자체**를 써서 링과 [러닝 시작] 버튼이 **같은 RGB(47,110,74)**.
색상각만 벌리면 색약(적록)에서 붙어 보이므로 **명도까지** 벌렸다.
포기한 것: 링에 팀색(그린)을 쓰는 직관적 매칭 — 브랜드 그린과 색상각 2° 차이라 불가.

**L2 · 성장 링 3등급을 색이 아니라 굵기로 표현** (메인 세션, R19 지적)
왜: tier 3이 `Brand.brand`를 쓰는데 `heroCam`(사진 변경 = **진짜 액션 버튼**)이 이 링에 **겹쳐**
그려져 정보와 액션이 한 덩어리로 보였다.
포기한 것: 새 색을 넣어 4번째 hue를 만드는 것("링 3색을 넘기지 말 것" 규칙 유지).

**L2 · 마스코트 이름을 2곳에만 노출** (메인 세션, 황태경 동의)
왜: 반복 노출 지점에 1인칭을 쓰면 3050 남성에게 **유치함이 누적**된다(서지안 지적과 같은 결론).
포기한 것: 빈 상태·온보딩에서 오키가 말을 거는 톤(따뜻하지만 소음이 된다).

**L2 · 한글 줄간격을 `leading()` 토큰으로 142곳 적용, 51곳은 의도적 제외** (메인 세션)
왜: `fontSize` 218곳 중 `lineHeight`가 23곳뿐이라 한글 받침이 붙어 보였다(회장 지적).
포기한 것: 전수 일괄 적용 — 입력칸은 안드로이드에서 글자가 잘리고, 단위·배지·버튼라벨은
단일행이라 효과 없이 박스만 커진다(특히 단위는 **중첩 inline Text**라 베이스라인이 흔들린다).

**L2 · 실기기 설치 경로를 WSL adb → Windows adb 로 전환** (메인 세션)
왜: usbipd 터널은 **50MB 설치가 6실패/3성공**, 무선 adb는 3~6분마다 끊김. Windows adb는 USB에
직접 붙어 **4초**.
포기한 것: WSL 안에서 모든 것을 처리하는 단일 환경(스크린샷을 `C:\`에 받아 읽는 우회가 생겼다).

**L2 · 웹 팔레트도 앱과 동일하게 반영, 단 배포는 보류** (메인 세션 · 회장 지시)
왜: 웹·앱이 다른 색을 쓰는 전환기 상태를 오래 두면 브랜드가 갈린다. 배포는 회장 지시("완성 후에").
포기한 것: 즉시 라이브 반영. 웹 서비스명("모두의 마라톤" → 5키로)·웹 마스코트는 범위 밖으로 남김.

**L2 · 디자인을 유지 모드로 전환 권고** (R19 채점자)
왜: 95점에서 남은 항목은 전부 좁은 범위·낮은 빈도의 잔여 이슈. 96점 이상은 `DESIGN.md`
"앱 디자인 큐"의 밀린 숙제(**공유 카드**·페이스+고도 차트·다크 모드)에서만 나온다.
포기한 것: 채점→수정→재채점 루프를 더 도는 것(수익 체감 구간).

---

## 2026-08-01 · 세션10

**L3 · 프로필 사진을 기기 로컬 → 서버 공유로 전환** (회장 결정 "크루가 서로 보게")
왜: 로컬 저장이면 크루원끼리 서로의 얼굴을 볼 수 없어 기능의 목적 자체가 없다.
포기한 것: "얼굴 사진이 서버에 안 올라간다"는 가장 안전한 상태. `profiles`를 공개 읽기로
열었으므로 **처리방침 개정이 선행**됐고, 크루 격리가 끝나기 전까지는 노출 범위가 "앱을 깐 전원"이다.

**L2 · 구글맵 파스텔 커스텀 스타일 폐기 → 거의 기본 지도** (회장 지적, 통근버스 앱 레퍼런스)
왜: 파스텔 스타일이 **지명·도로·물길을 다 지워 "어디를 달리는지"가 안 보였다.** 러닝 앱에서
지도는 장식이 아니라 정보다.
포기한 것: 브랜드 톤으로 통일된 지도 화면. 옛 스타일은 `MAP_STYLE_PASTEL`로 보존하되,
되돌릴 때 반드시 지킬 규칙 2개(지명·물길은 끄지 않는다)를 주석으로 박았다.

**L2 · 마스코트 성별 구분을 헤어스타일 → 소품(리본)으로** (회장 지시)
왜: 양은 머리카락이 아니라 **울로 덮인 동물**이라 헤어스타일 구분이 형태와 싸운다.
리본은 울 위에 얹히는 소품이라 충돌이 없고, **머리 윤곽 밖으로 삐져나와 36px 아바타에서도
실루엣이 갈린다**(목 리본은 정면에서 작아 그 크기에서 사라진다).
포기한 것: AI 재생성. 변형은 전부 코드로 파생해 실루엣 100% 동일을 보장한다.

**L2 · 공유 카드를 SVG 절대좌표로 만들고 산술 검산 스크립트를 필수화**
왜: RN과 달리 SVG엔 **레이아웃 엔진이 없어 넘친 글자가 조용히 잘린다.** 실기기 빌드는
한 사이클이 길어 눈으로 잡으면 늦다. 실제로 20자 러너 네임이 헤더와 충돌하는 것을 빌드 전에 잡았다.
포기한 것: 눈대중. 글자 폭은 폰트 `hmtx` 실측값(`lib/text-metrics.ts`)으로만 계산한다.

**L2 · AAB 제출 전 라이브러리가 병합하던 권한 4개 차단**(마이크·오버레이·카메라·부팅)
왜: 러닝 앱이 마이크·카메라를 요구하면 심사에서 정당화를 요구받고 설치 전환율도 떨어진다.
배경 위치도 제거 — 러닝 중 화면을 꺼도 되는 기능은 아직 없다.
포기한 것: 나중에 백그라운드 트래킹을 넣을 때 권한을 다시 신청해야 한다.

**L3 대기 · 크루 데이터 격리 구조** (적대회의 `/bs` 6명·3라운드 → `docs/BRIEF.md`)
채택 후보 = **B안(서브컬렉션) + 마이그레이션 폐기**. 결정은 오너 몫이라 착수하지 않았다.
왜 이렇게 갔나: **결정을 막던 미해결이 5분짜리 측정 하나로 사라졌다.** 레드팀이 "B안 실행
가능성이 이 숫자에 달렸는데 3라운드 동안 아무도 안 쟀다"고 지목한 gallery를 재보니 **0건,
전체 13건**. B안 치명 3건이 전부 마이그레이션에서 나왔으므로 함께 무효가 됐다.
포기한 것: A안(crewId 필드) — 복합 색인 7개가 필요한데 `firestore.indexes.json`도 배포
경로도 없고, **에뮬레이터가 색인을 자동 생성해 누락을 절대 못 잡는다**(`exists()` 10회 상한도
마찬가지). 자동 테스트 0개인 저장소에서 "프로덕션 첫 실행에서 죽는" 실패 모드는 감당할 수 없다.
살아남은 반대: 웹 랜딩이 crewId가 없어 사회적 증거를 잃는다 · SEED가 신규 크루마다 영구 발동한다
· 크루 안 동명이인은 A·B 어느 쪽도 못 막는다.

**절차 교훈 · 회의 자료에 실측 수치를 먼저 넣는다**
왜: 3라운드를 소모한 최대 쟁점(마이그레이션 비용)이 **측정 한 번으로 통째로 사라졌다.**
회의가 스스로 뒤집은 판정도 2건 나왔는데 둘 다 **코드를 안 열고 내린 것**이었다
(SEED의 하드코딩 날짜, 프로필 문서 id가 여전히 이름이라는 사실).
포기한 것: 없음. P1 자료 배포 시점에 실측을 넣는 비용이 라운드 하나보다 훨씬 싸다.

---

## 2026-08-01 · 세션11

**L2 · 출시 문서의 거짓 고지 3건을 코드가 아니라 문서 쪽에서 정정**
왜: 플레이 데이터 안전 양식은 **거짓 신고가 앱 삭제 사유**다. 프로필 사진이 서버 공유로
바뀌었는데 표가 "기기 로컬"로 남아 있었고, 민감정보인 심박이 고지 없이 공개되고 있었으며,
처리방침 §6은 "Rules가 무단 읽기를 제한한다"고 적혀 있었다(읽기는 하나도 제한하지 않는다).
포기한 것: "우리는 안전하다"고 읽히는 문장. 읽기가 열려 있다는 사실과 그 이유를 처리방침에
그대로 적었다. 크루 격리가 끝나면 다시 개정한다.

**L2 · `@firebase/rules-unit-testing` 하네스를 `app/`이 아닌 별도 패키지로**
왜: peer가 firebase^12인데 앱은 **v10에 묶여 있다**(metro package exports 회피 — 건드리면
앱이 즉사). 하네스는 앱 코드를 import 하지 않고 `firestore.rules` 파일만 읽으므로 버전을
갈라도 잃는 게 없다.
포기한 것: 단일 패키지. 앱을 firebase 12로 올릴 때 합치는 것을 검토한다.
부수 효과: **자바가 두 갈래가 됐다** — 안드로이드 빌드 JDK17 / 에뮬레이터 JDK21+.
`tests/run.sh`가 자기 프로세스 안에서만 21을 앞세운다(시스템 java를 올리면 APK 빌드가 깨진다).

**L2 · 격리 전 "지금 뚫려 있다"를 통과하는 테스트로 박제**
왜: 회귀 테스트는 보통 "막혔다"를 지키는데, 여기서는 **read가 전부 열려 있다는 사실 자체**를
테스트로 고정했다. 크루 격리를 적용하면 그 블록이 **실패하는 것이 정상**이고, 실패 목록이 곧
"격리가 실제로 닫은 문"의 증거가 된다. 통과하면 격리가 안 된 것이다.
포기한 것: 없음. 같은 방식으로 run.ts 우회 경로와 profiles 동명이인 덮어쓰기도 박제했다.

**L2 · 초대 코드는 엔트로피가 아니라 `get`/`list` 분리로 지킨다** (`docs/CREW-GATE.md`)
왜: 규칙엔 레이트 리밋이 없어 40비트도 시간을 벌 뿐이다. `invites/{code}`에
`allow get: if true; allow list: if false;`를 두면 **열거 자체가 불가능**해진다.
방금 깐 하네스로 실증했다 — 단건 get은 통과, 컬렉션 조회도 where 역추적도 거부.
**앱 코드를 한 줄도 쓰기 전에 설계를 검증했다.**
포기한 것: 코드를 crews 문서 안에 넣는 편한 구조(크루를 읽으면 코드가 딸려 나온다).

**L2 · `events.ts` 자동 시딩 제거**
왜: 일회성 이관 장치가 영구 코드로 남아 있었다. 날짜가 하드코딩이라 이미 전부 지난 모임이고,
크루 격리 후엔 **새 크루마다 남의 동네 지난 모임을 영구 재생성**한다.
포기한 것: 컬렉션이 비면 뭐라도 보이는 상태. 빈 목록이 정답이고 빈 상태 UI가 이미 있다.

**L2 · 아바타 사진의 36px 하한 제거** (실기기 검증에서 발견)
왜: 마스코트 선화 때문에 만든 하한이 **사진에 그대로 걸려 있었다.** 이 앱에서 36px 이상인
Avatar는 마이 탭 히어로와 시상대뿐이라, 크루원이 서로를 매일 보는 자리(홈 소식 30·댓글 30·
랭킹 행 34·참석자 22)에서는 **사진을 등록해도 이니셜만 보였다.** 2026-07-31 L3 결정이
시상대 3등 안에 든 사람에게만 이행되고 있었다.
포기한 것: 없음. 30px 실기기 확인 결과 사진은 뭉개지지 않는다(선화가 아니라서).

**절차 · 캡처가 통째로 검게 나오는 함정**
왜: 이 폰은 대기 중 화면이 꺼져 `screencap`이 **새까만 PNG**를 만든다. 라벨은 멀쩡한데
내용이 없는 캡처가 생긴다 — 세션8·9가 겪은 "캡처=라벨 불일치"와 같은 종류의 사고다.
→ 캡처 스크립트가 **매번 WAKEUP을 먼저 보낸다**(`scratchpad/shot.sh`).
포기한 것: 없음.

**결론(세션11 종합) · "범용성(웹·앱·워치 하나의 데이터)"과 크루 격리는 정면으로 충돌한다**
왜: 지금 다 연결돼 보이는 이유가 **모든 데이터가 전원에게 열려 있어서**다. 격리는 그 개방성을
닫는 일이므로 격리하는 순간 연결의 일부가 반드시 끊긴다. B안에서 **웹은 어느 크루를 봐야 할지
모른다**(= L3 ②의 정체). 워치는 앱을 거쳐 들어오므로 영향 없다.
결정 대기: **범용성의 정의를 먼저 못 박아야 한다** — "전원이 한 덩어리"면 격리가 그것을 깨고,
"각 크루 안에서 웹·앱·워치가 하나"면 양립한다. 후자면 B안이 맞고 **웹에 크루 로그인**이 필요하다.
포기한 것: 아직 없음. 다만 이 정의를 미루면 A/B 선택이 계속 겉돈다 — 두 안의 차이가 결국
"웹을 어느 쪽에 둘 것인가"로 수렴하기 때문이다.

---

## 2026-08-01 · 세션12

**L2 · 거리 링이 포화된 뒤는 "마일스톤"이 대신 말한다** (공유 카드 + 목록 썸네일)
왜: 링은 `RING_FULL_KM=10`에 `Math.min(…,1)` 클램프라 **10km·하프·풀코스가 완전히 같은
그림**(꽉 찬 원 + 12시 골드점)이었고 링 안쪽은 비어 있었다. 인스타에서 카드를 본 사람은 밑의
숫자를 읽기 전에 그림부터 보는데, 그 그림이 42km와 10km를 구분하지 못했다 — **자랑의 크기가
그림에 안 실린다.** 처방 셋: ①꽉 차면 링이 **골드**(10km 완주는 성과 = 골드 규칙에 부합)
②비어 있던 링 안쪽에 **10K / 하프 / 풀코스** ③꽉 찬 링에서는 12시 출발점을 찍지 않는다.
포기한 것: **10km 미만의 라벨(5K)**. 넣어 봤다가 뺐다 — 60%짜리 초록 링 안의 "5K"는 카드
아래의 `6.40 km`와 어긋나 보여 **라벨이 이 러닝의 거리로 오독된다.** 꽉 찬 골드 원 안에
들어갔을 때만 메달의 각인처럼 읽힌다. 5km 자랑은 배지 카드(5K 클럽)가 이미 맡고 있다.

**L2 · 마일스톤 기준을 공식 거리 정각(42.195)이 아니라 42.0 / 21.0으로**
왜: 수동 입력은 "42.19"로 들어오고 GPS도 접선 오차로 흔들린다. 정각을 요구하면 **실제
완주자가 라벨을 못 받는다.** 반대로 41km를 풀코스라 부르면 거짓이므로 공식 거리의 −0.5%에서
끊었다. 10은 앱 배지(10K 러너)와 같은 값이라 앱 안에서 눈금이 하나로 유지된다.
포기한 것: 공식 거리와의 문자적 일치.

**L2 · 마일스톤 라벨은 골드가 아니라 본문색(`ink`)**
왜: 크림 배경(`#f9f1e4`) 위 골드(`#c0841a`)는 **2.86:1** — 큰 텍스트 완화 기준(3:1)에도
미달한다. 세션8(다크 위 블루 3.06:1)·세션9(딥올리브 위 골드 4.30:1)과 **같은 함정**이라 같은
방식으로 막았다. 성과 신호는 링이 골드로 내고, 글자는 읽히는 게 먼저다.
포기한 것: 골드 글자의 화려함.

**L2 · 걷기는 골드·마일스톤에서 제외** (카드·썸네일 양쪽)
왜: 이 앱에서 걷기는 랭킹·배지에서 빠지는 기록이다. 12km를 걸었다고 골드를 주면 골드가
뜻을 잃고, 걸어서 "풀코스"는 거짓말이 된다.
포기한 것: 걷기 장거리의 시각적 보상.

**L1 · 삭제 다이얼로그의 파란색 = 시스템 기본값이 아니라 우리가 칠한 옛 브랜드색**
왜: 진단이 틀려 있었다(JN.md는 "시스템 테마라 colorAccent 설정 필요"로 적었다). 사실
`plugins/withBrandDialogTheme.js`가 이미 colorAccent를 심고 있었고 **값만 `#2563c9`(옛 Azure
Blue)로 남아** 있었다 — 2026-07-30 크림+그린 전면교체가 여기까지 오지 않은 것이다. 생성된
`android/app/src/main/res/values/colors.xml`이 그 값을 그대로 들고 있는 것으로 실증했다.
포기한 것: 없음. **근본 원인은 이 파일이 팔레트 단일 소스 밖에 있다는 것** — 플러그인은 빌드
시각에 node로 실행돼 TS 토큰을 import 할 수 없어 값을 손으로 복제한다. 그 사실을 파일 맨
위에 ⚠️로 박아 다음 팔레트 변경 때 같이 바뀌게 했다.

**절차 · 캡처 신선도를 초 단위로 검사한다** (세션11 교훈의 확장)
왜: 세션11은 "검은 캡처"를 WAKEUP으로 막았는데, 이번엔 **pull이 조용히 실패하자 이전 세션의
같은 이름 파일이 그대로 복사됐다.** 라벨은 맞고 내용은 옛것인 캡처 — 세션8·9·11이 세 번 겪은
사고의 네 번째 변종이다. → 캡처 스크립트가 (a)오류를 숨기지 않고 (b)폰의 캡처 파일을 매번
지우고 (c)**받은 파일의 mtime이 90초 이내인지 검사**한다. 아니면 실패로 끝낸다.
포기한 것: 없음.

**L3 · 데이터 구조를 "크루 격리 2층" → "가시성 3층(나만/크루/전체 공개)"으로** (JJUN님)
왜: 골프존·GDR 모델 제시 — "혼자 쓰는 앱이 우선, 친구로 추가한 사람이 랭킹에, 공개한 사람의
데이터만 공용 목록에". GDR의 **우리 매장 / 전국 매장** 2축이 이 앱의 **크루 / 전체 공개**와
정확히 대응한다. 이 구조가 3라운드를 소모한 **A안/B안 논쟁을 소멸시킨다** — 층이 곧 경로면
B안이 구조적으로 결정된다. 그리고 **웹이 갈 곳을 되찾는다**(공개 층을 그대로 본다).
포기한 것: "웹·앱·워치가 전원 한 덩어리"라는 범용성의 정의. 대신 **사용자가 층을 고른다**로
바꿨다 — 이게 세션11이 못 박으라고 한 ⓪의 답이다. 설계 정본 `docs/TIERS.md`.
부수 효과: **L3 3건 중 2건이 함께 소멸했다.** ②웹 사회적 증거 = 3층이 살리므로 결정 불필요,
③profiles id = 1층 경로가 `users/{uid}/…`라 uid로 자동 확정. 남은 오너 작업은 App Check
콘솔 등록(물리작업) 하나다.

**L2 · "친구"를 임의 그래프가 아니라 크루(그룹)로 흡수** (JJUN님 수용)
왜: 임의 친구 그래프는 **무료 플랜에서 규칙으로 지킬 수 없다.** Firestore 규칙은 필터가 아니고
(쿼리 결과를 걸러주지 않는다), `where(uid,'in',[…])`은 30개 상한 + **클라이언트가 목록을
조작하면 아무나 읽힌다**. 규칙이 검증하려면 친구 수만큼 `get()`이 필요한데 10회 상한이다
(A안이 무너진 것과 같은 함정). 팬아웃은 Spark 쓰기 2만/일을 먹고, Functions는 Spark 불가.
**그룹은 멤버십 문서 하나라 `get()` 1회로 끝난다.** GDR의 "우리 매장"도 임의 그래프가 아니라
소속 그룹이다.
포기한 것: 골프존식 연락처 친구. 러닝은 같이 뛰는 단위가 크루라 그룹이 더 자연스럽다.

**L2 · 연락처 접근 대신 초대 코드** (JJUN님 수용)
왜: 목적은 "친구를 찾는 것"인데 수단의 값이 너무 비싸다 — `READ_CONTACTS`는 플레이 민감
권한(Prominent Disclosure 의무), 매칭하려면 **전화번호를 수집**해야 해 처리방침을 다시 써야
하고(거짓 고지 3건을 방금 정정한 참이다), 데이터 안전 양식에 항목이 추가되며, 현재 Auth는
익명이라 전화번호 인증은 플랜 확인까지 따라온다. **초대 코드는 이미 설계·검증돼 있다**
(`CREW-GATE.md`). 러닝 크루엔 이미 단톡방이 있어 카톡 코드 전달이 권한 허용보다 마찰이 낮다.
포기한 것: 연락처 자동 매칭의 편의.

**L2 · 전체 공개는 opt-in, 크루는 기본 공개** (JJUN님 수용)
왜: **GPS는 비거리와 다르다.** ①경로는 집 주소를 드러낸다(대부분 집에서 출발해 집으로
돌아오므로 시작·끝점이 겹치는 곳이 거주지 — Strava가 프라이버시 존을 파는 이유). 우리는
경로를 서버에 저장하지 않아 지금은 안전하고, **공유 카드에 실을 때만** 판단이 필요하다.
②심박은 민감정보다. ③처음 뛰는 사람의 느린 페이스가 자동으로 전국에 올라가면 기록을 안 남기게
된다 — 확산에 역효과. ④플레이 데이터 안전의 "다른 사용자에게 표시됨"이 켜져 심사 리스크가 는다.
크루는 초대 코드로 스스로 들어온 곳이라 기본 공개가 맞다(= GDR 우리 매장).
포기한 것: GDR식 자동 공개의 활기.

**L2 · 2·3층은 이동이 아니라 사본** (`TIERS.md` 4-2)
왜: 이동 방식은 공개 취소가 delete→create인데, **중간에 실패하면 기록이 순삭된다** —
`BRIEF.md` R2(`run.ts:141~`)에서 이미 진단한 사고와 **정확히 같은 모양**이다. 사본이면 공개
취소가 사본 delete라 원본을 안 건드린다. 러닝 문서는 수백 바이트라 2~3벌도 무의미한 크기고,
동기화는 멱등 `put`이 이미 있다.
포기한 것: 단일 사본의 단순함. **갤러리 사진은 예외** — base64라 수백 KB이므로 한 층에만 둔다.

**L2 · 3층은 새 컬렉션이 아니라 현행 루트를 재활용** (`TIERS.md` 4-1)
왜: 기존 13건이 이미 루트에 있어 **마이그레이션이 0**이고, 웹의 전체 구독이 그대로 3층 구독이
되며, 규칙 diff가 작다(read는 유지, create에 공개 동의 검증만 추가). 덤으로 `ownerUid`가
생기면서 지금 전면 개방인 `delete: if true`를 **소유 기반으로 닫을 수 있다** — 현행 규칙 주석이
"Auth 도입 시 교체"라고 예고해 둔 항목의 회수다.
포기한 것: 없다. 단 **루트 13건은 공개 동의를 받은 적이 없으므로 컷오버 때 비우고 재입력**한다.
